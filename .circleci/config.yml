version: 2
jobs:
  build:
    docker:
      - image: jekyll/builder:pages
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: "Test Website"
          command: /usr/gem/gems/html-proofer-3.9.1/bin/htmlproofer src --allow-hash-href --check-html --empty-alt-ignore --disable-external
